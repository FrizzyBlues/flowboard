## Variant: V2.2 — Flip Calibration + Top Search + Room Rail

Combines everything approved so far:
- V2 sticker cards, fixed 320px height
- Calibration collapsed by default; clicking flips the card CONTENT between
  controls view and calibration view INSIDE the same fixed box — no overlap onto
  neighboring cards, no grid reflow, no resizing
- V1-style top toolbar: FILTER ROOM / ENTITY search + ARM BULK / OPEN ALL / CLOSE ALL
- V3-style left rail: per-room filter buttons with counts + offline badge

Interactive: search, room filter, vent toggles, calibration flip, SAVE feedback, ARM BULK.
